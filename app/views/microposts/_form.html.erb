<%= form_for(@micropost, remote: true, url: micropost_path(params[:id])) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :content, placeholder: @micropost.content, remote: true%>
  </div>
<% end %>
